===============================================================================
IMPORTANTE - Acompanhe seu dado veja etapa por etapa para você entender melhor.
===============================================================================

1. Montar a lógica para realizar a troca do tema escuro para o claro, criado no arquivo (toggle-mode).
2. Todo o arquivo foi importado diretamente para o arquivo principal, (main.js).

3. Foi importado o estado inical 'state' da aplicação no arquivo (index.js).
4. Criada a função para definir o estado do temporizador em termos de minutos e segundos no arquivo (index.js).
5. Função importada no arquivo (main.js) e criada a execução da função.

6. Criar os eventos de clique nos botões
7. Configurar o timer